<h1><%= @list.name %></h1>

<hr>
<h4> All the movies in the list</h4>

<ul class="list-group">
  <% @list.movies.each do |movie| %>
    <li class="list-group-item">
      <%= movie.title %>
    </li>
  <% end %>
</ul>

<hr>

<%= link_to 'New list', new_list_path %>
<%= link_to 'Add a movie', new_list_path %>
